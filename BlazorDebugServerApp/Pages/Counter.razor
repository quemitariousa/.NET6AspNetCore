@page "/counter"
@using System.Linq.Expressions

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>



<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@*
TODO: Test debug 
1. Set a breakpoint on the currentCount++
2. Run debug
3. In the browser, open Counter page and click on the button 
4. Track how the counter value changes after clicking count in the browser

TODO: Test debug with Exception 
1. Set a breakpoint on the currentCount++
2. Run debug
3. In the browser, open Counter page and click on the button more than three times to trigger an error
*@



@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
        //TODO: Go to implementation .razor
        //1. Right click on currentCount and select Go to ... Implementation
        //2. The cursor will move to the place where you initialized the variable
        if (currentCount > 3)
        {
            throw new Exception("Exception");
        }
    }

}