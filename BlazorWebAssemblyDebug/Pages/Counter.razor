@page "/counter"

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>


@*
TODO: Test debug counter
1. Set a breakpoint on the currentCount++
2. Run debug
3. In the browser, open Counter page and click on the counter button 
4. See the counter rendered in the browser

TODO: Test debug with Exception 
1. Set a breakpoint on the currentCount++
2. Run debug
3. In the browser, open Counter page and click on the button more than three times to trigger an error
*@

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
        if (currentCount > 3)
        {
            throw new Exception("Exception");
        }
    }

}